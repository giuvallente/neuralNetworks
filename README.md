# Artificial Neural Networks and Deep Learning

Comando para transformar o notebook em um script Python:

```bash
jupytext --to py exercise3.ipynb
```
Comando para atualizar o submodule para versão mais recente:

```bash
git submodule update --remote --merge neuralNetworks
git add neuralNetworks
git commit -m "Update submodule"
git push
```
